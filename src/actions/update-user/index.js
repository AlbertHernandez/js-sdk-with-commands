const { UpdateUserCommandHandler } = require('./update-user-command-handler')
const { UpdateUserCommand } = require('./update-user-command')

module.exports = {
  UpdateUserCommandHandler,
  UpdateUserCommand
}
